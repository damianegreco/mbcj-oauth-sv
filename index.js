const oauthRouter = require('./oauthRouter');
const middleware = require('./middleware');
const obtenerClavePublica = require('./obtenerClave');

module.exports = {oauthRouter, middleware, obtenerClavePublica}